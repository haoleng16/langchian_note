针对文档生成的如何将用户解析为主题,风格等
  class RequirementAnalysis(BaseModel):
      """需求分析结果"""
      topic: str = Field(description="PPT主题")
      slide_count: int = Field(description="幻灯片数量")
      style: str = Field(description="PPT风格，如商务、学术、创意等")
      target_audience: str = Field(description="目标受众")
      key_points: List[str] = Field(description="需要包含的关键点")



